{% extends "base.html.twig" %}

{% block body %}

<div class="row align-items-start">
    <div class="col">
        <div class="alert" id="zoneFormulaire">
            {{ form(form) }}
        </div>

        {% if competitors is defined %}
        <div class="alert" id="zonePosition">
            Classement
            <div>{{ position }}/{{ nbResults }}</div>
        </div>
        {% endif %}
    </div>

    {% if competitors is defined %}
    <div class="col">
        <div class="alert" id="zoneResultat">
            <table class="table">
                <thead>
                    <tr>
                        <th>Prix de vente</th>
                        <th>Vendeur</th>
                        <th>Etat du produit</th>
                    </tr>
                </thead>
                <tbody>

                {% for competitor in competitors %}
                    <tr class="{% if newCompeitorName == competitor.competitor %} table-info {% endif %}">
                        <td>{{ competitor.price|number_format(2, '.', ' ') }} €</td>
                        <td>{{ competitor.competitor }}</td>
                        <td>{{ competitor.conditionProduct }}</td>
                    </tr>
                {% endfor %}                
                </tbody>
            </table>
        </div>
    </div>
    {% endif %}
</div>
{% endblock %}